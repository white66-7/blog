## 引言
这是我写好这个网页首页后的第一篇文章，可以说是热情程度最高的时候，因此置顶这篇带着饱满热情写的文章。

在写这个网页前其实我还写过另一个小网页，[点击显示](/messages/1/1.webp)。

原因呢就是当时一直用网易云，但是我呢又比较喜欢听周杰伦的稻香，晴天，搁浅等等，这就很矛盾。巧的是，某天刷b站刷到一个介绍html文档的视频，一闪而过的音乐播放界面瞬间给了我启发，后来我开始学这类知识，开始第一次写这类代码。

麻雀虽小，五脏俱全，虽然是一直被群嘲的localhost，但是初学者写起来也是蛮难的。因为它包含数据库交互，数据仓库管理，状态管理，状态刷新以及我认为最难理解的Preview对象。

后来我觉得那个播放器网页实在是不好维护，换句话说就是又臭又长。本着精益求精的想法，在recat和vue这两个框架中我选择了vue进行学习，老实说我上手很快，因为我发现vue的核心设计和C++的类与对象(面向对象编程)非常像，而我之前写的两个小游戏的基石就是类与对象。学完一些vue3基本的特性，我就想找个项目来练手，最后从零开始写了这个网页。

### 傻了
我在6月22号这天突然发现，原来只要把歌曲拖进网易云就能放进网易云的个人云盘，这样来说我其实根本不用搞这个音乐播放器。哎呀，这波纯属信息偏差了。如果你缺周杰伦的音乐文件，我就给你个夸克网盘的地址吧[]()
## 网页的初次尝试
当我一开始写网站时，我头皮发麻，一是我没有找到main函数入口，之前写的所有程序，都是在main函数中循环，触发设定的退出if结束程序，触发你的某些if走向不同的运行道路。但是网页的运行给我一种多线程错觉，即网页在同一时间能够处理多件事情，这几乎颠覆我的代码思路。

那么真的有那么玄幻吗，其实并非。这里引入一个概念，浏览器引擎是有监视器的，鼠标点击操作，滚轮划动，以及网页加载，网页卸载都是能被监视到的，main函数不过是换了一种形式，运用监视器在加载网页时触发main函数。至于多线程这回事我实在是难以讲明白,但是我觉得当你想明白时，你就已经明白了。
## vue框架的核心设计
vue3框架究竟做了什么改善呢，打个比方，起始的框架是整体的，像是用线编织一个人偶，当你要改动一处时，往往你是需要拆开一大部分，在剩余完善的部分重新开始织，而vue3框架的时分散的，像是用乐高搭这个人偶，搭好腿,手,头等等，再整合在一起,如果改动就可以把手拆下来单独重新改，改好后再装回去。另外，以前写两个网页是需要两个html文件的，代码更是臭上加臭。而vue3则利用组件式设计达到易维护，易修改的优势。

为了实现这些目的，就需要牵扯出vue3的响应式设计和路由设计，每个vue项目都有一个“路由器”用来管理不同的线路
```javascript
import { createRouter, createWebHistory } from 'vue-router'
const routes = [
{
  path: '/',
  name: 'blog',
  component: Bloghome
},
{
  path: '/qq',
  name: 'qq-contact',
  component: QQContact
  },]
const router = createRouter({
  history: createWebHistory(),
  routes,
export default router
})
```
其中path就是指具体的每条路径,compoent对应的就是组件,如果你要跳转网页那么他会自动切换你对应网页的组件呈现,而原来的组件一般都会被销毁,那么在这里我提个疑问以供思考，我的的音乐播放器是怎么做到在多个页面的跳转中实现歌曲不卡顿的呢，明明数据不是已经被销毁了吗，跳转回来的时候竟然如此丝滑，至于这个的原因，我想我会在下一篇具体讲解我这个网站的时候提及

那么什么是响应式设计呢，提两个最典型的特性，ref和computed,同时还不得不提数据特性paine,在原来的网页中，打个比方，歌曲点击下一首后，原来的网页通过监视器绑定，触发这个事件对应的数，然后这个函数内部还得包含别的函数，比如渲染函数，销毁封面，总时间，作者，歌名后显示新的数据。相比之下,vue通过直接绑定html文档中的数据显示,,用一个"形参"来代表显示数据，那么这个数据来源于哪里呢
是的，就是我之前提及的paine
```javascript
import { defineStore } from 'pinia'
export const usePlayerStore = defineStore('player', () => {
  const currentSong = ref({ title: '稻香', id: 1 })
  function nextSong() { ... }
  return { currentSong, nextSong }
})

import { useLibraryStore } from '@/stores/libraryStore'
const libraryStore = useLibraryStore()
```
通过导出一个形参usePlsyStore,这个形参包含return的所有内容,数据,处理函数,然后这个对象在具体的组件中被导入后接着就被引用
```javascript
 <div class="song-title">{{ currentSong.title }}</div>
 const currentSong = ref({
  title: '稻香',
  cover: '/covers/daoxiang.webp',
  id: 1
})
const currentLyrics = computed(() => {
  return currentSong.value.id
})
  function nextSong() {
  const next = getNextSong()
  currentSong.value = next
  }
```
当你点击按钮触发nextSong()后，currentSong的值被修改了，computed属性的currentLyrics检测到它依赖的currentSong 变了，自动重新计算，更新歌曲有关的显示
## 网页的基本布局
先简答介绍一下何为盒子模型,组件式的逻辑与这个具有强关联,在html文档中标明一个class属性(假设class = "box-A"),接下来在css中引用他给他设计属性,对于基础属性宽高，盒子模型将其分为三层。第一层是内容区，显示你的文字，svg图标等等,第二层是内边距区,即padding,边界的一个外扩，可以让按钮的可触面积变大，让字体在更大的可显示空间,一般情况都要使用他，第三层是外边距，即是margin,标记距离下一个盒子模型的距离，当两个盒子模型相邻的时候一个他们之间的间距不是外边距的叠加，而是取两个之中更大值。而边框border则是纯纯的修饰性属性,可以把他看作文字的描边属性,突出显示
```css
  .box-A {
  width: 100px;
  height: 40px;
  background-color: #007bff;
  color: white;
  padding: 20px 30px; 
  margin-bottom: 30px; 
  border-radius: 8px;
  }
```
接下来就是怎么在网页中布局这个难点了,怎么设计呢?我在这里就拿我的音乐播放器举例,可以看到很明显的分区,右侧一个整体是唱片区，左侧则是一整个歌曲管理区，而在左侧的歌曲管理区中从上至下有不同的组件盒子，比如显示卡片，搜索区，导入区，还有歌曲区(如下图)
<img src="/messages/1/1.webp" style="width: 100%;" />
具体思路就是嵌套思路，首先设置整个网页为一个盒子，然后在这个大盒子中，再设置两个盒子，这两个盒子按照display属性进行排列，这里为了让他成为左右排列，有两个思路，第一个思路是让这个大盒子成为一个display为flex(弹性布局),这个弹性布局会默认让小盒子拉伸为全部高度，接着给一个盒子设置宽度，然后另一个flex属性为1说明占据剩余的空间
```css
<div class="father">
  <div class="left">左</div>
  <div class="right">右</div>
</div>
<style>
  .father {
    display: flex; 
    height: 100%; 
  }
  .left {
    width: 300px;
  }
  .right {
    flex: 1;
  }
</style>
```
那么接下来就是在left这个块级容器中操作了，首先将left这个盒子的display属性也设为flex,接着设置对齐属性flex-direction,而colume(柱)指的是所有元素将从上至下排列，用gap属性设置每个盒子的默认间距
```css
<div class="left">
  <div class="brand">🎵 音乐都市</div>
  <div class="now-playing">▶ 正在播放：等待信号</div>
  <div class="search">⌕ 搜索框</div>
  <div class="list-area"></div>
</div>
<style>
 .left {
    display: flex;
    flex-direction: column;   
    gap: 14px;              
    width: 300px;
  }
  .brand { height: 50px; }
  .now-playing { height: 70px; }
  .search { height: 44px; }
  .list-area {
    flex: 1;                
    overflow-y: auto; 
  }
  </style>
```
接着就是对每个小组件的设置了，给每个盒子合适的高度，接着给歌曲列表(list-area)设置flex:1这样他就占据了剩余的所有空间，overflow-y: auto即当元素(歌曲数量)过多时自动存在滚动条
我认为事实上理解完这些内容，自己就不能再算是前端代码的小白，在此之后我已经可以理解每个网页布局格式并且能够写出来样式很简单的网页，开启丛零开始的进阶之路！
## 对大学的感慨
老实说,这些内容并不是学校教学的内容，拿我学校的培养方案来说，大一学到数据结构，大二开始学java的基本语法还有基本的数据库了解关于后端交互的一些逻辑，大三才有web开发的课程，和网页强相关。五年前的程序员最好的路线是专攻前端后端,如果想要多赚点可以转到架构亦或是算法工程师，但是这些需要更多的本事和技能,需要编程类竞赛证书,多段实习经历。现如今就业前景已经大为不同了,让我真正感到恐怖的，不是那些GitHub上大佬们开发出的skills，也不是最近短视频平台整天嚷嚷的codex，claudecode，而是Ai恐怖的进化能力。就在一年以前我高考结束,大模型的能力还平平无奇,换句话说，在当时的人眼里,他还只是一个高级的搜索引擎。一年之后的现在,Ai的算法，基座不知更新迭代优化了多少次，已经拥有恐怖的上下文理解能力，做到真正生成高质量视频，模型等一系列资源文件，那么3年之后呢，Ai又能进化到什么程度呢，所以我现在几乎每次刷到计算机相关的视频和帖子几乎都会点不感兴趣，那些高赞视频要么挑起对立，要么引发焦虑，浪费精力在这样的事情上面在我看来实在不值。每个人都面临不同的处境，不能靠别人的所谓圣经来告诉你答案。我5月份买了一本书《认知觉醒》，花了1个半月还没读完,但是里面的内容确实很新奇有说法，借用里面的概念来说，在我的3月到4月中，我其实都蛮焦虑的，我觉得我没学什么东西，我的情绪脑一直告诉我完蛋了，但直到当我真正去做，可能我的理智脑才真正觉醒，在写代码和文章的时候，我的心没那么浮躁了，那些真正吸收进东西的感觉成了我焦虑的代餐。

代码不知为何对我很吸引，我真的很喜欢这种造东西的感觉。这可能源我是个很怕别人评价的人，而代码是默不作声慢慢建筑的，并且一些代码的事情无论别人说是ai写的亦或者好强这类的话我都觉得其实没什么，因为放在他们的角度其实不是很懂我到底在干什么，我也不用特别去在意思考。

在大学这一个人生进程中，有人会选择努力保研，有人会选择按部就班，有人会选择享受生活。老实说，我未来其实不想再读研究生了，不想再被栓在学校，做一些子虚乌有的学术。或许现实来说不是个好选择，但我觉得至少这是一条适合我的路。在科研这条路上我也建议你去看看这样的两篇文章，第一篇我是有亲身体验的，而第二篇我认为推导过程严密，加上现在的学术共识，是值得被相信的[本科生科研](https://shitspace.xyz/article/27686af2-9dd7-4ab5-9741-72c52e4a424b?from=%2Fsearch%3Fq%3D%25E6%259C%25AC%25E7%25A7%2591%25E7%2594%259F)，[研究生科研。](https://shitspace.xyz/article/e7121782-8ffb-4f2c-8c14-877aea5fcb12?from=%2Farticle%2F27686af2-9dd7-4ab5-9741-72c52e4a424b%3Ffrom%3D%2Fsearch%25253Fq%3D%25E6%259C%25AC%25E7%25A7%2591%25E7%2594%259F)

希望未来你我在各自道路上都无限进步！！！