## 基本思路
我觉得面对算法题我的基本思路第一想法就是尝试两个技术手段DFS和BFS。如果这不行就再去根据这题的情境和我的理解试试其他的特殊算法，比如动态规划，差分,二分什么的。

因为这学期学了数据结构,现在看这些也清楚了一些。
## 回顾DFS和BFS
DFS和BFS是一种遍历手段,主要使用于树这个数据结构。其实是一种经过优化后的暴力手段。举个我当时理解入门的一个例子来说，给定一个迷宫，有很多错综复杂的路，老鼠从入口进入迷宫后只能通过暴力的搜索所有可能的道路，知道找到一个出口。

DFS： 可以看作是二叉树的正序遍历，这只老鼠每次遇到分叉路都往左边转。假设某一次左转后碰壁，那么就返回刚才的岔路口，这一次走右边，下次还是选择走左边，概括来说就是：一路到底，逐步回退。

BFS： 老鼠召唤无数的分身,在每个路口都派部分老鼠去探路,并且标记已经走过，如果碰壁或是碰到已经被标记的路就停下，概括来说就是: 全局出击，逐层递进。

BFS相比于DFS的优势在于能够最快输出最短路径。而在其他情况下这两个方法各有千秋。
## DFS框架和应用情境
dfs的实现手段是递归，理解起来有一定的难度。先给出框架吧
```C++
void dfs(...){
    if(到达目的地或是超出边界){
        更新答案;
        return;
    }
    for(用i来遍历下一层的所有可能)
    {
        if(visit[i] = 0) //只探索已经走过的路
        {
            visit[i] = 1; //标记
            dfs(...) //前往下一层
            vist[i] = 0; //!!! 恢复状态防止这层另外一种可能
        }
    }
    return;
}
```
最需要花费心力去理解的是visit[i] = 1后再vist[i] = 0这两个表达式。需要明白我注释的意思。

接下来解一个经典例题n皇后：n个皇后放在n X n的棋盘上，并且让皇后彼此之间不能互相攻击，即皇后不能在同一列，一行，一条对角线上。
那么怎么求出所有解呢?

先看框架后看调用。
```C++
int n,num;
int ans[20];
int vis[30];
int vis1[30];
int vis2[30];
void dfs(int x){
    if(x == n + 1) // 上一次已经完整
    {
        num++;
        return;
    }
    for(int i =0 ;i< n;i++)
    {
        if(vis[i])  continue;//只探索已经走过的路
        if(vis1[x + y]) continue; //主对角线标记检查
        if(vis[x + n -y]) continue; //副对角线标记检查
        ans[x] = i;
        vis[i]= 1;  vis1[x + y] = 1;  vis[x + n -y] = 1;
        dfs(x+1);
        vis[i]= 0; vis1[x + y] = 0; vis[x + n -y] = 0;
    }
}
int main()
{
    cin>>n;
    dfs(1); //从第一层开始算
    cont num;
    return 0;
}
```
这里其实难点在于能否想到标记主对角线和副对角线的方法,我的理解是可以看作坐标系中y = x和y = -x这两条线。一条线的横坐标和纵坐标其实满足某种条件。
## BFS框架和应用情境
dfs的实现手段是队列，借助它先进先出。
```c++
void bfs()
{
    queue<int> q; //队列
    q.push(root);
    while(!q.empty)
    {
        int u = q.front()
        cout << t[u];
        q.pop();
        if (到达目的地) {
            更新答案
            return; // 通常可直接返回
        }
        for (遍历所有下一层状态) {
            if (边界合法 && visit[next] == 0) {
                visit[next] = 1;    // 标记已访问
                q.push(next);       // 入队
            }
        }
    }
    return;
}
```
例题是：马的遍历

有一个n X m的棋盘，在点(x,y)有一个马，要求马到达棋盘每一个点最少要几步。
```c++
int n,m;
struct node{
    int x,int y;
}
int dis[1000][1000] //存储最短距离
int vis[1000][1000] //标记数组
int dx[8] = {2,2,1,1,-1,-1,-2,-2}
int dy[8] = {1,-1,2,-1,2,-2,1,-1}
node s;
void main()
{
    cin >> n >> m >> s.x >> s.y;
    messet(dis,-1,sizeof(dis));
    dis[s.x][s.y] = 0;
    vis[s.x][s.y] = 1;
    queue<int> q; //队列
    q.push(s);
    while(!q.empty)
    {
        int now = q.front();
        q.pop();
        for (int i = 0;i<8;i++) //遍历所有下一层状态
        {
            node next;
            next.x = now.x + dx[i];
            next.y = now.y + dy[i];
            if(next.x < 0 || next.x > n) continue
            if(next.y < 0 || next.y > m) continue
            vis[next.x][next.y] = 1 //标记
            dis[next.x][next.y] = dis[now.x][now.y] + 1;
            q.push(next) 
        }
    }
    return;
    for(int i =0 ;i< n;i++)
    {
        for(int j = 0;j<m;j++)
        {
            print('%d\n',dis[i][j]);
        }
    }
}
```
## 前缀和 
在输入时就进行和计算，可以省下很多时间

一维的来说：sum[i] = sum[i-1] + 输入的值

二维的来说：sum[x][y] =  sum[x][y-1]+ sum[x-1][y] -sum[x-1][y-1] + 输入的值